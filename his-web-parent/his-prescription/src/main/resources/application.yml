logging:
  level:
    cn:
      bdqn:
        his: DEBUG
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: cn.bdqn.his.prescription.entity
oauth2:
  server:
    url: http://localhost:9001
security:
  oauth2:
    client:
      access-token-uri: ${oauth2.server.url}/oauth/token
      client-id: client
      client-secret: secret
      user-authorization-uri: ${oauth2.server.url}/oauth/authorize
    resource:
      token-info-uri: ${oauth2.server.url}/oauth/check_token
    sso:
      login-path: /login
server:
  masterdata:
    url: http://localhost:9007
  medicine:
    url: http://localhost:9006
  port: 9005
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION2
spring:
  application:
    name: his-prescription
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 1
      initial-size: 3
      max-active: 10
      max-wait: 60000
      min-idle: 3
      stat-view-servlet:
        login-password: admin
        login-username: admin
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/his_prescription?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: his
  session:
    timeout: 3600000
  thymeleaf:
    cache: false
swagger2:
  enable: true
  title: 就诊模块
